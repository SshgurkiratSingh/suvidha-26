# Suvidha Frontend

## API Base URL

Set a single frontend parameter to switch backend environments:

```sh
VITE_API_URL=http://localhost:4000
```

If `VITE_API_URL` is not set, the app defaults to `http://localhost:4000`.
